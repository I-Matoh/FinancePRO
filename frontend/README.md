# SecurePay Frontend

## Stack
React + TypeScript + React Router + React Query + Tailwind.

## Setup
1. Copy `.env.example` to `.env` and set `VITE_API_URL`.
2. Install dependencies and run:

```powershell
npm install
npm run dev
```
